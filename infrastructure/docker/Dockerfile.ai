FROM pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime

WORKDIR /app

COPY requirements.ai.txt .
RUN pip install --no-cache-dir -r requirements.ai.txt

COPY . .

EXPOSE 8501

CMD ["python", "-m", "ai-engine.inference_orchestrator"]
